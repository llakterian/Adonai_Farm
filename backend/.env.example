# Backend environment example (do not commit real secrets)
# Copy to backend/.env and fill in values.

# Server
PORT=4000
NODE_ENV=development

# JWT
JWT_SECRET=change_me

# Database (optional: if present, backend runs Postgres mode)
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Email (Gmail recommended: use 16-char App Password)
SMTP_HOST=smtp.gmail.com
# 587 with STARTTLS (SMTP_SECURE=false) or 465 with SSL (SMTP_SECURE=true)
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@gmail.com
SMTP_PASS=your_16_char_app_password
# Optional custom From
# SMTP_FROM=Adonai <you@gmail.com>

# CORS / Frontend
FRONTEND_URL=http://localhost:5173