run = "cd backend && npm install && npm run migrate && npm start"
modules = ["nodejs-18"]

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "cd backend && npm install && npm run migrate && npm start"]